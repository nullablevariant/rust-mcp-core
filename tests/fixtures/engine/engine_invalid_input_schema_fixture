config:
  version: 1
  server:
    host: 0.0.0.0
    port: 3000
    endpoint_path: /mcp
    logging:
      level: info
    transport:
      mode: streamable_http
    auth:
      enabled: false
  tools:
    items:
      - name: tool.ok
        description: invalid schema
        input_schema: "nope"
        execute:
          type: http
          upstream: api
          method: GET
          path: /