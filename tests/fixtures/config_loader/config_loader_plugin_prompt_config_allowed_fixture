version: 1
server:
  host: "127.0.0.1"
  port: 3000
  endpoint_path: "/mcp"
  logging:
    level: "info"
  transport:
    mode: streamable_http
  auth:
    enabled: false
plugins:
  - name: "prompt.plugin"
    type: prompt
    config:
      region: "us"
tools:
  enabled: false
  items: []