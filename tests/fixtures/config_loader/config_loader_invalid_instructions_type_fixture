version: 1
server:
  host: "127.0.0.1"
  port: 3000
  endpoint_path: "/mcp"
  logging:
    level: "info"
  transport:
    mode: streamable_http
  auth:
    enabled: false
  info:
    instructions:
      text: "not-a-string"
tools:
  items:
    - name: "tool.noop"
      description: "No-op tool"
      input_schema:
        type: object
      execute:
        type: http
        upstream: api
        method: GET
        path: /